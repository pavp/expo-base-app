# Lockfiles are generated and have their own canonical format. Formatting
# pnpm-lock.yaml rewrites every `resolution:` block, so the next `pnpm install`
# reformats it back — a ~4800-line diff on any change that touches dependencies.
pnpm-lock.yaml

# Generated by expo prebuild; not ours to format.
android/
ios/
.expo/

# Build output and coverage reports.
coverage/
node_modules/
