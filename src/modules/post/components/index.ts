// `PostsVerticalCarousel` moved into `@/modules/feed/components` in Phase B Slice 4a. This
// barrel stays only to keep `home-view`/`explore-view` compiling until Slice 4b absorbs them too.
// Do not add anything new here — new feed code belongs in `@/modules/feed`.
export { PostsVerticalCarousel } from '@/modules/feed/components';
